# Рабочие файлы, которые init создаёт на машине.
# В репозиторий едут только *.example — реальные значения остаются локально.
env
*.env
!*.env.example
!env.example

# Локальные алиасы машины живут в каталоге конфигов, а не здесь.
local.just

# Бэкапы, которые оставляют init и just_new.
*.bak
TZ.md
