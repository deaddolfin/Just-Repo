# Заглушки переменных группы «example».
# init склеивает этот файл с Config/env.example в <каталог конфигов>/env;
# реальные значения заполняются там и в репозиторий не возвращаются.

# короткое имя хоста из ~/.ssh/config для рецептов ssh/restart
H=example-host
